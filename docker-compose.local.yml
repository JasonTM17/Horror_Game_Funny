# Local overrides: bind-mount the working tree for faster iteration without rebuild.
# Usage:
#   docker compose -f docker-compose.yml -f docker-compose.local.yml run --rm suite

services:
  suite:
    volumes:
      - ./:/app
    environment:
      GODOT: /usr/local/bin/godot
